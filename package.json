{
  "name": "homehd",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "build-data": "tsx scripts/build-data.ts",
    "deploy:web": "cd flutter && flutter build web --release && cd .. && firebase deploy --only hosting --project homehd-personal",
    "deploy:api": "cd api && gcloud run deploy homehd-api --source . --region=europe-west2 --allow-unauthenticated --project=homehd-personal"
  },
  "devDependencies": {
    "csv-parse": "^6.2.1",
    "tsx": "^4.19.0"
  }
}
